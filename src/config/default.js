/* eslint-disable no-script-url */
const config = {
  show: 'javascript:void($zopim.livechat.window.show())',
  pulse: {
    frameDurationMs: 100,
    framesInTick: 5,
  },
};

export default config;
